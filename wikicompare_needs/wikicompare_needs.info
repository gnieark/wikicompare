name = Wikicompare Needs
description = Add the possibility to manage group of needs, which we can then select in the table to have custom results.
package = Wikicompare
version = VERSION
core = 7.x
dependencies[] = wikicompare